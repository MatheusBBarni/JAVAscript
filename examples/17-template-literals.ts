function main(): void {
  const name: string = "Alice";
  const age: number = 30;
  const msg: string = `Hello ${name}, you are ${age} years old!`;
  const nested: string = `${msg} And learning Java!`;
  console.log(nested);
}

main();
